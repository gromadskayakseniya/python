x = int(input("Введите целое положительное число: "))
reversed_x = 0
temp = x
if x < 0:
    print("Неверные данные")
else:
    while temp > 0:
        reversed_x = reversed_x * 10 + (temp % 10)
        temp //= 10

    print("Число в обратном порядке:", reversed_x)
