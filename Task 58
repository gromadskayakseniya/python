N = int(input("Введите число: "))
a = list(map(int, input().split()))

last_index = -1
for i in range(1, N - 1):
    if a[i] < a[i - 1] and a[i] < a[i + 1]:
        last_index = i

if last_index == -1:
    print("nо")
else:
    print(last_index)
