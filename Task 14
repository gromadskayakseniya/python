y = float(input("Градусов: "))

# Часовая стрелка проходит 30 градусов за 1 час (360 / 12)
hours = int(y // 30)
rest_degrees = y % 30
# Часовая стрелка проходит 0.5 градуса за 1 минуту (30 / 60)
minutes = int(rest_degrees // 0.5)

print("Часов:", hours)
print("Минут:", minutes)
